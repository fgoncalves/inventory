package com.fred.inventory.presentation.navigation;

/**
 * Encapsulate all the callbacks that can be called when navigation events can happen
 */
public interface NavigationListener {
  /**
   * Called when the user clicks the add product list button
   */
  void onAddProductListButtonClicked();
}
