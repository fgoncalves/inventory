<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
  <data>
    <variable
        name="viewModel"
        type="com.fred.inventory.presentation.productlist.viewmodels.ProductListRecyclerViewItemViewModel"
        />
  </data>

  <android.support.constraint.ConstraintLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="16dp"
      android:layout_marginStart="16dp"
      android:layout_marginLeft="16dp"
      android:layout_marginRight="16dp"
      android:layout_marginEnd="16dp"
      >

    <ImageView
        android:id="@+id/product_list_product_image"
        android:layout_width="0dp"
        android:layout_height="180dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/placeholder_groceries"
        />

    <View
        android:id="@+id/view2"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/black_gradient"
        app:layout_constraintBottom_toBottomOf="@+id/product_list_product_image"
        app:layout_constraintLeft_toLeftOf="@+id/product_list_product_image"
        app:layout_constraintRight_toRightOf="@+id/product_list_product_image"
        app:layout_constraintTop_toTopOf="@+id/product_list_product_image"
        />

    <TextView
        android:id="@+id/product_list_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="16dp"
        android:text="@{viewModel.productName()}"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/product_list_product_image"
        app:layout_constraintLeft_toLeftOf="@+id/view2"
        app:layout_constraintRight_toLeftOf="@+id/product_list_item_quantity"
        tools:text="Bertoli"
        style="@style/MainTitleText"
        />

    <TextView
        android:id="@+id/product_list_item_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:text="@{viewModel.productQuantityLabel()}"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintBaseline_toBaselineOf="@+id/product_list_item_name"
        app:layout_constraintRight_toRightOf="@+id/product_list_product_image"
        tools:text="200 g"
        style="@style/MainTitleText"
        />
  </android.support.constraint.ConstraintLayout>
</layout>
