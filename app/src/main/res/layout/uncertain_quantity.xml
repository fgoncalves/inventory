<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
  <data>
    <variable
        name="viewModel"
        type="com.fred.inventory.presentation.items.viewmodels.ItemViewModel"
        />
  </data>

  <android.support.constraint.ConstraintLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      >

    <SeekBar
        android:id="@+id/seekBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:max="@{viewModel.uncertainQuantityMaximumObservable()}"
        app:progress="@{viewModel.quantityObservable()}"
        />

    <com.fred.inventory.presentation.widgets.clicktoedittext.ClickToEditTextViewImpl
        android:id="@+id/max_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:addTextChangedListener="@{viewModel.uncertainQuantityMaximumTextWatcher()}"
        android:digits="0123456789"
        android:gravity="center"
        android:inputType="number"
        android:maxLines="1"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/quantity_unit"
        app:layout_constraintRight_toLeftOf="@+id/quantity_unit"
        app:layout_constraintTop_toTopOf="@+id/quantity_unit"
        tools:text="200"
        style="@style/MainTitleText"
        android:hint="@string/total_quantity_text_hint"
        />

    <com.fred.inventory.presentation.widgets.clicktoedittext.ClickToEditTextViewImpl
        android:id="@+id/quantity_unit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:maxLines="1"
        android:text="@{viewModel.uncertainQuantityUnitObservable()}"
        android:textStyle="bold"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/seekBar"
        tools:text="Kg"
        android:hint="@string/quantity_unit_text_hint"
        style="@style/MainTitleText"
        />
  </android.support.constraint.ConstraintLayout>
</layout>
